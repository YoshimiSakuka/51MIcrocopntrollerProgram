/**********************SY-1实验开发板例程************************
*  平台：SY-1+STC89C52
*名称：以独立按键中K1按键为例，示例独立键盘的使用方法。
			用led指示HL-1实验板键盘最下面一排左侧的K1键的状态。
			当按键按下时，led亮；松开后，灭。
*  公司：思源电子科技有限公司
*  编写：思源
*  日期：2012-12-25
*   QQ : 936559219
*  晶振:11.0592MHZ
******************************************************************/


#include<reg51.h>

sbit k1=P3^4;
sbit led=P1^0;

void main()
{
	while(1)		   //通过大循环不断扫描按键状态
	{
		if(k1==0)
			led=0;
		else
			led=1;
	}
}